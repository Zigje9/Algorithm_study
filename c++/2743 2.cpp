#include <iostream>
#include <string>

using namespace std;
string alphabet;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> alphabet;
    cout << alphabet.size() << "\n";
    return 0;
}