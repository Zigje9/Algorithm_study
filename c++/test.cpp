#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){

    int A = 1;
    if(A<2){
        cout << "2";
    }
    if(A<3){
        cout << "3";
    }

    // vector<int> v; // vector

    // v.push_back(10);
    // v.push_back(20);
    // v.push_back(30);
    // v.push_back(22);

    // sort(v.begin(), v.end());

    // vector<int>::iterator iter = lower_bound(v.begin(), v.end(), 11); // iterator

   

    // cout << v[iter-v.begin()] << endl;


    return 0;

}